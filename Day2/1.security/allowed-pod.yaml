apiVersion: v1
kind: Pod
metadata:
  name: allowed-nginx
  namespace: default
  annotations:
    openshift.io/scc: restricted-scc
spec:
  serviceAccountName: secure-sa
  securityContext:
    runAsUser: 1000
  containers:
    - name: nginx
      image: bitnami/nginx
      imagePullPolicy: IfNotPresent
      securityContext:
        allowPrivilegeEscalation: false
      volumeMounts:
        - name: config-volume
          mountPath: /etc/nginx
  volumes:
    - name: config-volume
      emptyDir: {}